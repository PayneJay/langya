<?xml version="1.0" encoding="utf-8"?>
<resources>
    <attr name="textSize" format="dimension" />
    <attr name="textColor" format="color" />
    <attr name="expandedText" format="string" />
    <attr name="collapsedText" format="string" />
    <attr name="trimLines" format="integer" />

    <!--显示更多内容-->
    <declare-styleable name="MoreTextView">
        <attr name="expandedText" />
        <attr name="collapsedText" />
        <attr name="trimLines" />
        <attr name="textSize" />
        <attr name="textColor" />
    </declare-styleable>

    <!--折叠显示的TextView-->
    <declare-styleable name="CollapsedTextView">
        <attr name="expandedText" />
        <attr name="collapsedText" />
        <attr name="trimLines" />
    </declare-styleable>

    <!--自定义下载进度-->
    <declare-styleable name="CirclesProgressBar">
        <attr name="circlesWidth" format="dimension" />
        <attr name="circlesColor" format="color" />
        <attr name="textCrude" format="dimension" />
        <attr name="textColor" />
        <attr name="textSize" />
        <attr name="currentProgressColor" format="color" />
        <attr name="currentProgress" format="integer" />
        <attr name="isPercent" format="boolean" />
        <attr name="style" format="integer" />
        <attr name="currentScheduleWidth" format="dimension" />
    </declare-styleable>

    <!--仿新浪体育点赞进度条-->
    <declare-styleable name="RatioProgressBar">
        <attr name="left_progress_value" format="integer" />
        <attr name="left_progress_bg" format="color" />
        <attr name="right_progress_value" format="integer" />
        <attr name="right_progress_bg" format="color" />
        <attr name="left_right_progress_spacing" format="integer" />
        <attr name="progress_anim_duration" format="integer" />
        <attr name="progress_height" format="dimension" />
    </declare-styleable>

    <!--自定义抽屉控件-->
    <declare-styleable name="Panel">
        <!-- Defines panel animation duration in ms. -->
        <attr name="animationDuration" format="integer" />
        <!-- Defines panel position on the screen. -->
        <attr name="position">
            <!-- Panel placed at top of the screen. -->
            <enum name="top" value="0" />
            <!-- Panel placed at bottom of the screen. -->
            <enum name="bottom" value="1" />
            <!-- Panel placed at left of the screen. -->
            <enum name="left" value="2" />
            <!-- Panel placed at right of the screen. -->
            <enum name="right" value="3" />
        </attr>
        <!-- Identifier for the child that represents the panel's handle. -->
        <attr name="handle" format="reference" />
        <!-- Identifier for the child that represents the panel's content. -->
        <attr name="content" format="reference" />
        <!-- Defines if flying gesture forces linear interpolator in animation. -->
        <attr name="linearFlying" format="boolean" />
        <!-- Defines size relative to parent (must be in form: nn%p). -->
        <attr name="weight" format="fraction" />
        <!-- Defines opened handle (drawable/color). -->
        <attr name="openedHandle" format="reference|color" />
        <!-- Defines closed handle (drawable/color). -->
        <attr name="closedHandle" format="reference|color" />
    </declare-styleable>

    <!--带非空校验的自定义EditText-->
    <declare-styleable name="HunterEditText">
        <attr name="isRequired" format="boolean" />
    </declare-styleable>

    <declare-styleable name="ProgressImageView">
        <!--图片圆角大小-->
        <attr name="roundPixel" format="dimension|reference" />
        <!--是否启用进度-->
        <attr name="useProgress" format="boolean" />
        <!--进度阴影色值-->
        <attr name="roundShadowColor" format="color|reference" />
        <!--进度文字大小-->
        <attr name="roundProgressTextSize" format="dimension|reference" />
        <!--进度文字颜色-->
        <attr name="roundProgressTextColor" format="color|reference" />
        <!--圆环进度的宽-->
        <attr name="roundProgressWidth" format="integer" />
        <!--圆环进度的高-->
        <attr name="roundProgressHeight" format="integer" />
        <!--进度大小-->
        <attr name="progressMax" format="integer" />
    </declare-styleable>

    <!--仿iOS下载安装应用动画-->
    <declare-styleable name="CircleLoadingView">
        <attr name="cl_circleRadius" format="dimension" />
        <attr name="cl_fillAnimationDuration" format="integer" />
        <attr name="cl_circleStrokeSize" format="dimension" />
        <attr name="cl_src" format="integer" />
    </declare-styleable>
</resources>